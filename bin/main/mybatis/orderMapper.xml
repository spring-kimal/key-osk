<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="c.e.d.mapper.OrderMapper">
	<insert id="saveOrd" >
		Insert into `order` values (Null, Null)
	
		<selectKey keyProperty="ordNumber" resultType="int" order="AFTER">
		SELECT LAST_INSERT_ID();
		 </selectKey>
	</insert>
	
	<insert id="saveItemOrder" parameterType="c.e.d.data.ItemOrder" >
		Insert into item_order(itemQuantity,itemId,ordNumber) values(#{itemQuantity},#{itemId},#{ordNumber} )
		
		<selectKey keyProperty="itemOrderId" resultType="int" order="AFTER">
		SELECT LAST_INSERT_ID();
		 </selectKey>
	
	
	</insert>
	

	
</mapper>